# Generative Art Canvas

Build a generative art tool with three algorithms: Flow Field, Lissajous Curves, and Recursive Tree. Each has live parameter controls, Regenerate, Pause/Resume, and Save PNG.

- Implement `GenerativeCanvas`, algorithm classes, `perlinNoise.ts`, and control panel with sliders.
- Run: `npm install && npm run dev` — then `npm test`.
