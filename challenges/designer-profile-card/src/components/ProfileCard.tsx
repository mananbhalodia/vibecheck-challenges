export function ProfileCard() {
  return (
    <div className="starter-message" data-testid="profile-card">
      <p>
        Your goal is to recreate the target component shown in the reference
        image — a designer profile card with a photo, name, title, skill tags,
        stats, and action buttons.
      </p>
    </div>
  );
}
